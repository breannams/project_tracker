<div class = "content">
<h1 class= "is-size-1">Category: <%= @category.name %> - <%= @category.projects.count %> projects</h1>

<ul>
    <% @category.projects.each do |project| %>
        <li><%= link_to project.title, project %> </li>
    <%end%>
</ul>
</div>