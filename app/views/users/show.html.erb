<h1 class = "is-size-1">Welcome <%= @user.username %></h1>

<div class = "is-size-3">
<%= link_to "Create a new project!", new_project_path %>
<br>
<%= link_to "See all of your projects!", user_projects_path(current_user) %>
<br>
<%= link_to "See everyone's projects!", projects_all_path %> 
</div>


Your entries:
<% current_user.entries.each do |entry| %>
<ul>
<li>    
Entry's Notes: <%= entry.notes %>
<li>View this project: <%= link_to(entry.project.title, entry.project)%>
</li>
</ul>
<% end %>